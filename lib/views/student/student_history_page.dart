// lib/views/student/student_history_page.dart
import 'package:flutter/material.dart';

class StudentHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Student History'),
      ),
      body: Center(
        child: Text('This is the Student History Page'),
      ),
    );
  }
}
