import 'package:flutter/material.dart';

class StaffHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Staff History')),
      body: Center(
        child: Text('History of room management will be displayed here.'),
      ),
    );
  }
}
